# OpenTelemetryCollector Example

This example uses `OtlpTraceExporter` to directly emit traces to the `OpenTelemetryCollector` via gRPC.

## Run locally

```sh
make collector

# in separate terminal
swift run
```
